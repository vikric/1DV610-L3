{
  "name": "validator-app",
  "version": "1.0.0",
  "description": "1DV610 - Third assignment",
  "type": "module",
  "browser": "src/index.html",
  "scripts": {
    "dev": "vite",
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0",
    "test": "npx --node-options=--experimental-vm-modules jest  || exit 0"
  },
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@lnu/eslint-config": "^1.1.10",
    "vite": "^6.3.6"
  },
  "dependencies": {
    "validator-sv": "^2.0.8"
  }
}
